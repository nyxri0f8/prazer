import 'package:flutter/material.dart';

/// Design tokens for PRAZER as specified in prazer_design_prompt.md
class PrazerColors {
  // Palette A — Structural (background, surfaces, text)
  static const Color alabasterGrey = Color(0xFFE5E4E2);
  static const Color onyx = Color(0xFF0A0A0A);
  static const Color blueSlate = Color(0xFF536878);

  // Palette B — Accent (interaction only)
  static const Color coolHorizon = Color(0xFF58A6FF);
  static const Color grapefruitPink = Color(0xFFFF6B6B);
  static const Color papayaWhip = Color(0xFFFFEDD5);

  // Card Surfaces
  static const Color surfaceWhite = Color(0xFFFFFFFF);
  static const Color borderSubtle = Color(0x20536878);
  static const Color shadowSubtle = Color(0x0F0A0A0A);
}
